# OpenWebUI Service

OpenWebUI (Ollama edition) deployment.

EDWH tooling compliant - use `edwh setup; edwh restic.configure -c local` to also include a local backup. See `ew help restic.configure` for other destinations 

